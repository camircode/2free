{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core and native notification access for the main 2 Free window.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
